<!DOCTYPE html>
<html>
<head>
  <title>Home | Pokerhands</title>
</head>
<body>
<h1>Check a Poker Hand</h1>
<div>
  <%= form_for(@card, url: cards_path, action: "cards#create", html: {method: "post"}) do |f| %>
    <%#= render 'shared/error_messages' %>
    <%= stylesheet_link_tag "custom", :media => "all" %>
    <p><%= f.text_field :all_card, value: @card[:all_card],class: "form-control input-hg", placeholder: "S10 H11 D12 C13 H1" %></p>
    <p><button class="btn btn-hg btn-primary">
      <%#= f.submit("check") %>
      check
    </button></p>
<!--    <%# raise() %>>-->
    <% if @card.errors.any? %>
      <div id="error_explanation">
        <ul>
          <% @card.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  <% end %>
</div>
<h2>
  <%= @result %>
</h2>
</body>
</html>